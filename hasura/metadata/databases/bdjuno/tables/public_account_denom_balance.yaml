table:
  name: account_denom_balance
  schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - account
    - denom
    - amount
    filter: {}
  role: anonymous
